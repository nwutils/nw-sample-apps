var cmd;
if (process.platform == 'win32') {
  cmd = 'start';
} else if (process.platform == 'linux') {
  cmd = 'xdg-open';
} else if (process.platform == 'darwin') {
  cmd = 'open';
}

exports.execute = function(uri) {
  require('child_process').exec(cmd + ' "' + uri + '"');
}
